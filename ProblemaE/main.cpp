#include <iostream>

using namespace std;

int main()
{
    int k = 0, n, m, x, y;
    cin >> k;

    for (int i = 0; i < k; ++i)
    {
        cin >> n >> m;
        cin >> x >> y;

    }

    return 0;
}